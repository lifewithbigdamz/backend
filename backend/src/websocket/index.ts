import { ClaimGateway } from './claim.gateway';

export function setupWebSocket(server: any) {
  // This function will be used to initialize the WebSocket gateway with the HTTP server
  // For NestJS, this is typically handled by the framework, but for hybrid setups, you can manually attach
  // Example: ClaimGateway instance can be created and initialized here if needed
}
